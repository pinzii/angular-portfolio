import { FancyDirective } from './fancy.directive';

describe('FancyDirective', () => {
  it('should create an instance', () => {
    const directive = new FancyDirective();
    expect(directive).toBeTruthy();
  });
});
